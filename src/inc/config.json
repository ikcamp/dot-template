
{
  "name": "dot-template",

  "data": {
    "rootPath": {
      "type": "string",
      "description": "项目根目录路径"
    },
    "npmPath": {
      "type": "string",
      "description": "node_modules 目录路径"
    },
    "date": {
      "type": "string",
      "description": "当前日期"
    },
    "time": {
      "type": "string",
      "description": "当前时间"
    },
    "datetime": {
      "type": "string",
      "description": "当前日期与时间"
    },
    "user": {
      "type": "string",
      "description": "系统用户，读取环境变量中的 HOME"
    },
    "pkg": {
      "type": "{[key: string]: any}",
      "description": "项目根目录上的 package.json 文件的内容"
    },

    "filePath": {
      "type": "string",
      "description": "当前文件的绝对路径"
    },
    "relativeFilePath": {
      "type": "string",
      "description": "当前文件相对根目录的路径"
    },
    "fileName": {
      "type": "string",
      "description": "当前文件的名称，不带路径和后缀"
    },
    "fileExt": {
      "type": "string",
      "description": "当前文件的后缀"
    },
    "dirPath": {
      "type": "string",
      "description": "当前文件的目录的绝对路径"
    },
    "dirName": {
      "type": "string",
      "description": "当前文件的目录的名称"
    },
    "rawModuleName": {
      "type": "string",
      "description": "和 fileName 一致"
    },
    "moduleName": {
      "type": "string",
      "description": "fileName 的驼峰形式"
    },
    "ModuleName": {
      "type": "string",
      "description": "fileName 中的每个单词首字母都大写"
    },
    "MODULE_NAME": {
      "type": "string",
      "description": "fileName 中所有字母都大写，并用下划线连接"
    },
    "module_name": {
      "type": "string",
      "description": "fileName 中所有字母都小写，并用下划线连接"
    },
    "ref": {
      "optional": true,
      "type": "IData",
      "description": "创建 related 文件时，原文件的 data 对象"
    }
  },

  "commands": {
    "createTemplateFile": {
      "title": "DTPL: Create template files",
      "key": "ctrl+k ctrl+p",
      "mac": "cmd+k cmd+p"
    },

    "createRelatedFile": {
      "title": "DTPL: Create related file",
      "key": "ctrl+k ctrl+s",
      "mac": "cmd+k cmd+s"
    },
    "rollbackCreates": {
      "title":"DTPL: Rollback or recover created files",
      "key": "ctrl+k ctrl+u",
      "mac": "cmd+k cmd+u"
    }
  },

  "options": {
    "dtplFolderName": {
      "type": "string",
      "default": ".dtpl",
      "description": "文件夹的名称，用于存放模板文件及相关配置文件"
    },
    "dtplExtension": {
      "type": "string",
      "default": ".dtpl",
      "description": "指定 dtpl 模板文件的后缀名"
    },
    "ejsExtension": {
      "type": "string",
      "default": ".ejs",
      "description": "指定 ejs 模板文件的后缀名"
    },
    "njkExtension": {
      "type": "string",
      "default": ".njk",
      "description": "指定 nunjucks 模板文件的后缀名"
    },

    "minimatchOptions": {
      "type": "object",
      "default": {
        "matchBase": true,
        "nocomment": true,
        "dot": true
      },
      "description": "minimatch 的选项，用于匹配模板名称, 参考：https://github.com/isaacs/minimatch#options",
      "properties": {
        "nobrace": {
          "type": "boolean",
          "description": "Do not expand {a,b} and {1..3} brace sets"
        },
        "noglobstar": {
          "type": "boolean",
          "description": "Disable ** matching against multiple folder names"
        },
        "dot": {
          "type": "boolean",
          "description": "Allow patterns to match filenames starting with a period, even if the pattern does not explicitly have a period in that spot"
        },
        "noext": {
          "type": "boolean",
          "description": "Disable 'extglob style patterns like +(a|b)"
        },
        "nocase": {
          "type": "boolean",
          "description": "Perform a case-insensitive match"
        },
        "matchBase": {
          "type": "boolean",
          "description": "If set, then patterns without slashes will be matched against the basename of the path if it contains slashes"
        },
        "nocomment": {
          "type": "boolean",
          "description": "Suppress the behavior of treating # at the start of a pattern as a comment"
        },
        "nonegate": {
          "type": "boolean",
          "description": "Suppress the behavior of treating a leading ! character as negation"
        },
        "flipNegate": {
          "type": "boolean",
          "description": "Returns from negate expressions the same as if they were not negated. (Ie, true on a hit, false on a miss.)"
        }
      }
    }
  }
}
